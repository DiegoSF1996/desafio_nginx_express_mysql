
events {
    worker_connections 1024;
}
http {

    server {
    
        listen 80;
        
        server_name node;

        location / {
            proxy_pass http://node_app;
            
        }
    }
    
}